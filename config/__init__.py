"""Paquet racine de la configuration du projet Django.

Contient les modules ``settings`` (base / env / security / logging / third_party),
``urls`` (routage racine), ``wsgi`` et ``asgi`` (points d'entrée des serveurs
d'application).
"""
