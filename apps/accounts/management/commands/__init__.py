"""Commandes ``manage.py`` propres à l'app accounts.

Exemple : ``createsuperadmin`` — provisionne un super-administrateur initial
sans passer par le shell interactif (utile en démarrage / CI / Docker).
"""
