"""
FICHIER : apps/notifications/urls.py
RESPONSABILITE : Routes URL pour les notifications (vide - gere via API)
"""
from django.urls import path

app_name = "notifications"  # <--- Change le nom pour chaque app (ex: 'absences', etc.)

urlpatterns = [
    # Laisse vide pour l'instant
]
