# =============================================================================
# Production runtime dependencies
# =============================================================================
# All transitive dependencies are resolved by pip automatically. Only direct
# project dependencies are pinned here, plus a few framework helpers that are
# listed explicitly because they are part of the public Django/DRF contract.
#
# Platform markers:
#   - python-magic relies on libmagic (apt: libmagic1). The Docker image and
#     CI runner install it as a system package. On Windows it is still
#     installed but the runtime import is wrapped in a guarded try/except
#     (see apps/absences/utils_upload.py).
# =============================================================================

# --- Django core --------------------------------------------------------------
Django==6.0.5
asgiref==3.11.0
sqlparse==0.5.5
tzdata==2025.3

# --- Django apps & middleware -------------------------------------------------
crispy-bootstrap5==2025.6
django-crispy-forms==2.6
django-csp==4.0
django-filter==24.3
django-ratelimit==4.1.0
django-redis==6.0.0
djangorestframework==3.16.1
drf-spectacular==0.29.0

# --- Database, cache, server --------------------------------------------------
gunicorn==25.1.0
psycopg2-binary==2.9.11
redis==7.3.0

# --- Application features -----------------------------------------------------
openpyxl==3.1.5
pillow==12.2.0
pyotp==2.9.0
python-dotenv==1.2.2
python-magic==0.4.27
qrcode==8.2
reportlab==4.4.10

# =============================================================================
# Test and quality tooling (used by CI and local development)
# =============================================================================
black==26.3.1
coverage==7.13.5
flake8==7.3.0
isort==7.0.0
pytest==9.0.3
pytest-cov==6.3.0
pytest-django==4.12.0
ruff==0.15.1
